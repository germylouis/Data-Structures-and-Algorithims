package SortingAlgorithims;

public class MergeSort {
    /*Recursion: Routine calls itself from within its own code based on termination condition.
    Way of solving a problem by dividing it into sub problems.
    Output of one recursion becomes input to the next. Factorial is a recursive function.

    Merge sort is all about Divide, Conquer and Combine. List is divided recursively into two halves until
    no division is possible. Merges the smaller list into new lists in sorted order.
    */
    public void sortInt(int arr[]) {

    }
}